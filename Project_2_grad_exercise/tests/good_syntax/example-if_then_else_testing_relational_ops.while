x := input;
if x = 0 then
  y := ((1 + 0)+0) * 1
else
  if [x > 0] and [x < 10] then
    y := 2
  else
    if [x >= 10] and [x <= 20] then
      y := 3
    else
      y := 4
    fi
  fi
fi;
output := y
